# Validation Report: macOS PyPI Release Pre-Submission Test

**Date:** June 9, 2026
**Package:** `weppcloud-wbt`
**Version:** `2.3.0.post1`
**Environment:** macOS (Apple Silicon), `uv` package manager

## 1. Executive Summary
The `weppcloud-wbt` package release `2.3.0.post1` was validated on macOS. All core unit tests pass, documentation examples (doctests) have been patched and verified, and the PyPI release was successfully installed and smoke-tested in a clean environment.

## 2. Unit & Integration Testing
Before verifying the PyPI release, the local codebase was audited.

- **Status:** PASS
- **Command:** `cargo test --workspace`
- **Total Tests:** 201 passed
- **Doc Test Fixes:** 
  - Several doc tests in `whitebox-common`, `whitebox-raster`, and `whitebox-vector` were failing due to missing imports or syntax errors. 
  - These were patched (Commit: `6dddecd8`) and verified to pass.

## 3. PyPI Release Verification
A clean installation was performed using `uv`.

### 3.1 Environment Setup
```bash
uv venv --python 3.11
source .venv/bin/activate
uv pip install weppcloud-wbt==2.3.0.post1
```

### 3.2 Smoke Test Results
- **Installation:** Success
- **Version Verification:** `WhiteboxTools v2.4.0` (forked from Dr. John Lindsay's WBT)
- **Tool Count:** 468 tools registered
- **Custom Tool Availability:**
  - `find_outlet`: Present
  - `fvslope`: Verified (via tool list)
  - `raise_roads`: Verified (via tool list)

### 3.3 Custom Tool Help Output
Verified the help output for the primary custom tool:
```bash
$ python -c "from weppcloud_wbt import WhiteboxTools; wbt = WhiteboxTools(); print(wbt.tool_help('find_outlet'))"
# Output confirmed correct parameters: --d8_pntr, --streams, --watershed, etc.
```

## 4. Conclusion
The `weppcloud-wbt` v2.3.0.post1 release is ready for submission and production use on macOS.

---
*Report generated by Gemini CLI*
